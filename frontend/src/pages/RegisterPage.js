import React from "react";
import Form from "../components/Form";

const RegisterPage = () => {
    return (
        <Form buttonName="Register" classType="register" heading="Register" request="register" />
    );
}

export default RegisterPage